# VibeDocs - GitHub 기반 인터랙티브 문서화 프레임워크

> 🎯 **한 줄 요약**: 아름답고 편집 가능한 문서 사이트를 만드는 프레임워크. 템플릿을 고르고, 콘텐츠만 관리하세요.

---

## 1. Purpose (프로젝트 목적)

### 1.1 Problem Statement

현재 문서화 솔루션들의 한계:

| 문제점            | 기존 솔루션                       |
| ----------------- | --------------------------------- |
| **밋밋한 디자인** | 기능 중심, 시각적 차별화 부족     |
| **읽기 전용**     | 편집하려면 Git 클론 → 수정 → Push |
| **개발자 중심**   | 비개발자 기여 진입장벽 높음       |

### 1.2 Solution

**VibeDocs**는 프레임워크로서:

- 사용자는 `docs/` 폴더와 `vibe.config.ts` 설정만 관리
- 내부 구현(컴포넌트, 라우팅, 빌드)은 프레임워크가 처리
- 테마 커스터마이징은 설정과 CSS 변수로 제공

```
사용자가 관리하는 것:
├── docs/           ← 마크다운 콘텐츠
├── public/         ← 정적 에셋 (로고, 이미지)
└── vibe.config.ts  ← 설정 파일

프레임워크가 처리하는 것:
├── 컴포넌트 (Header, Sidebar, DocPage...)
├── 라우팅
├── 빌드 시스템
├── GitHub OAuth
├── CRUD API
└── 애니메이션
```

### 1.3 Target Users

| 사용자 유형             | 니즈                  | VibeDocs 제공 가치 |
| ----------------------- | --------------------- | ------------------ |
| **오픈소스 메인테이너** | 예쁘고 관리 쉬운 문서 | 설정만으로 완성    |
| **스타트업**            | 빠른 문서 구축        | CLI 한 줄로 시작   |
| **비개발자 팀원**       | Git 없이 문서 기여    | 웹에서 직접 편집   |

### 1.4 Success Metrics

| 지표                   | 목표 (MVP) |
| ---------------------- | ---------- |
| GitHub Stars           | 500+       |
| npm 주간 다운로드      | 1,000+     |
| Lighthouse Performance | 90+        |

---

## 2. Core Value Proposition

### 2.1 경쟁사 비교

| 기능         | Docusaurus | GitBook   | VibeDocs     |
| ------------ | ---------- | --------- | ------------ |
| 정적 사이트  | ✅         | ❌        | ✅           |
| 웹 편집기    | ❌         | ✅ (유료) | ✅ (무료)    |
| 모던 디자인  | 보통       | 보통      | ✨ 핵심 강점 |
| 무료         | ✅         | 제한적    | ✅           |
| Git 히스토리 | ✅         | ❌        | ✅           |

### 2.2 핵심 차별화

1. **Designful** - 모던 UI, 부드러운 애니메이션
2. **Editable** - 브라우저에서 바로 CRUD
3. **Zero Config** - 템플릿 선택 후 콘텐츠만 관리
4. **Hybrid** - 읽기는 정적(빠름), 쓰기는 동적(편리함)

---

## 3. Templates (템플릿)

VibeDocs의 핵심 차별화 요소. 모든 템플릿은 React Bits, shadcn/ui, Aceternity UI 등을 활용한 인터랙티브 디자인.

### 3.1 템플릿 선택

```bash
npx create-vibe-docs my-docs

# ? Select a template:
# ❯ Minimal     - 깔끔하고 빠른 기본 템플릿
#   Developer   - 개발자 도구/라이브러리 문서에 최적화
#   Product     - SaaS/제품 문서 + 랜딩페이지
#   Knowledge   - 위키/지식베이스 스타일
#   API         - API 레퍼런스 특화
```

### 3.2 Official Templates

#### Minimal

> 깔끔함이 최고의 디자인

| 특징            | 설명                               |
| --------------- | ---------------------------------- |
| **컨셉**        | 콘텐츠 중심, 군더더기 없음         |
| **적합한 용도** | 개인 프로젝트, 심플한 문서         |
| **인터랙션**    | Subtle hover, 부드러운 페이지 전환 |
| **레이아웃**    | 좌측 사이드바 + 콘텐츠             |

````
┌─────────────────────────────────────────┐
│  Logo              Search    🌙  GitHub │  ← 미니멀 헤더
├──────────┬──────────────────────────────┤
│ Getting  │                              │
│ Started  │  # Welcome                   │
│          │                              │
│ Guide    │  Clean and simple docs.      │
│  ├ Intro │                              │
│  └ Setup │                              │
│          │                              │
│ API      │  ```code block```            │
│          │                              │
└──────────┴──────────────────────────────┘
````

**주요 컴포넌트**:

- Fade-in 애니메이션
- 스크롤 진행률 표시바
- 심플한 코드 블록 (복사 버튼)

---

#### Developer

> 개발자 도구 문서의 정석

| 특징            | 설명                                |
| --------------- | ----------------------------------- |
| **컨셉**        | 기술적 신뢰감, 개발자 친화적        |
| **적합한 용도** | 오픈소스 라이브러리, CLI 도구, SDK  |
| **인터랙션**    | 터미널 타이핑 효과, 코드 하이라이트 |
| **레이아웃**    | 사이드바 + 콘텐츠 + TOC             |

```
┌─────────────────────────────────────────────────────┐
│  ⌘ DevTool        Docs  API  Blog    ☆ Star 1.2k  │
├──────────┬─────────────────────────────┬───────────┤
│          │                             │ On this  │
│ 📦 Guide │  # Installation             │ page     │
│  ├ Quick │                             │          │
│  └ Setup │  ┌──────────────────────┐   │ • Install│
│          │  │ $ npm install devtool│   │ • Config │
│ 📘 API   │  │ $ devtool init  ▌    │   │ • Usage  │
│  ├ Core  │  └──────────────────────┘   │          │
│  └ Utils │     ↑ 타이핑 애니메이션      │          │
│          │                             │          │
└──────────┴─────────────────────────────┴───────────┘
```

**주요 컴포넌트**:

- 터미널 컴포넌트 (타이핑 효과)
- 다중 언어 코드 탭 (JS/TS/Python...)
- 버전 드롭다운
- GitHub Stars 배지
- 패키지 매니저 선택 (npm/yarn/pnpm)

---

#### Product

> 제품과 문서를 하나로

| 특징            | 설명                           |
| --------------- | ------------------------------ |
| **컨셉**        | 마케팅 + 문서 통합             |
| **적합한 용도** | SaaS, B2B 제품, 스타트업       |
| **인터랙션**    | Hero 애니메이션, 스크롤 reveal |
| **레이아웃**    | 랜딩페이지 + 문서 섹션         |

```
┌─────────────────────────────────────────────────────┐
│  Product     Features  Pricing  Docs     [Sign Up] │
├─────────────────────────────────────────────────────┤
│                                                     │
│         Build amazing things                        │
│           with our platform                         │
│                                                     │
│     ┌─────────────┐  ┌─────────────┐               │
│     │ Get Started │  │ Watch Demo  │               │
│     └─────────────┘  └─────────────┘               │
│              ↑ Gradient + Glow 효과                 │
├─────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│  │ Feature │  │ Feature │  │ Feature │            │
│  │   One   │  │   Two   │  │  Three  │  ← 카드 호버│
│  └─────────┘  └─────────┘  └─────────┘            │
└─────────────────────────────────────────────────────┘
```

**주요 컴포넌트**:

- Hero 섹션 (그라데이션, 파티클)
- Feature 카드 (호버 시 3D tilt)
- Testimonial 캐러셀
- CTA 버튼 (Glow 효과)
- Pricing 테이블

---

#### Knowledge

> 팀의 두뇌를 담는 공간

| 특징            | 설명                             |
| --------------- | -------------------------------- |
| **컨셉**        | 위키, 지식 관리, 내부 문서       |
| **적합한 용도** | 팀 위키, 온보딩 문서, 지식베이스 |
| **인터랙션**    | 문서 연결 그래프, 백링크         |
| **레이아웃**    | 검색 중심 + 카테고리 그리드      |

```
┌─────────────────────────────────────────────────────┐
│                  Knowledge Base                      │
│          ┌─────────────────────────────┐            │
│          │  🔍 Search documentation... │            │
│          └─────────────────────────────┘            │
├─────────────────────────────────────────────────────┤
│                                                     │
│   ┌───────────┐  ┌───────────┐  ┌───────────┐     │
│   │ 🚀        │  │ 📖        │  │ ⚙️        │     │
│   │ Getting   │  │ Guides    │  │ Reference │     │
│   │ Started   │  │           │  │           │     │
│   │ 12 docs   │  │ 24 docs   │  │ 18 docs   │     │
│   └───────────┘  └───────────┘  └───────────┘     │
│                                                     │
│   Recently Updated           Popular               │
│   ├ Onboarding Guide         ├ API Overview        │
│   ├ Code Standards           ├ Setup Tutorial      │
│   └ Deploy Process           └ FAQ                 │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**주요 컴포넌트**:

- 전체 검색 (Cmd+K)
- 카테고리 카드
- 문서 연결 그래프 (옵션)
- 백링크 표시
- 최근 업데이트 / 인기 문서

---

#### API

> API 문서의 새로운 기준

| 특징            | 설명                                 |
| --------------- | ------------------------------------ |
| **컨셉**        | API Reference 특화                   |
| **적합한 용도** | REST API, GraphQL, SDK 레퍼런스      |
| **인터랙션**    | 인터랙티브 API 테스터, 응답 미리보기 |
| **레이아웃**    | 3단 구조 (Nav + Doc + Code)          |

```
┌─────────────────────────────────────────────────────────────┐
│  API Reference         v2.1 ▼        Auth: Bearer ****     │
├────────────┬──────────────────────────┬─────────────────────┤
│            │                          │                     │
│ Auth       │ POST /users              │ ┌─────────────────┐ │
│ ├ Login    │                          │ │ Request         │ │
│ └ Logout   │ Create a new user        │ │ POST /api/users │ │
│            │                          │ │                 │ │
│ Users      │ Request Body:            │ │ {               │ │
│ ├ Create ● │ ┌────────────────────┐   │ │   "name": "Jo"  │ │
│ ├ Read     │ │ name*    string    │   │ │   "email": ".." │ │
│ ├ Update   │ │ email*   string    │   │ │ }               │ │
│ └ Delete   │ └────────────────────┘   │ ├─────────────────┤ │
│            │                          │ │ Response  200   │ │
│ Products   │ Responses:               │ │ {               │ │
│            │ 200: User created        │ │   "id": "123"   │ │
│            │ 400: Validation error    │ │   "name": "Jo"  │ │
│            │                          │ │ }               │ │
│            │          [Try it] ←──────│ └─────────────────┘ │
└────────────┴──────────────────────────┴─────────────────────┘
```

**주요 컴포넌트**:

- HTTP 메서드 뱃지 (GET/POST/PUT/DELETE)
- 파라미터 테이블
- Request/Response 코드 블록
- "Try it" 인터랙티브 테스터
- 인증 토큰 입력

---

### 3.3 템플릿별 UI 라이브러리 활용

| 컴포넌트 유형     | 라이브러리        | 사용 템플릿   |
| ----------------- | ----------------- | ------------- |
| 버튼, 폼, 모달    | **shadcn/ui**     | 전체          |
| 터미널 효과       | **React Bits**    | Developer     |
| 카드 호버/3D      | **Aceternity UI** | Product       |
| 텍스트 애니메이션 | **React Bits**    | Product, Hero |
| 파티클/배경       | **Aceternity UI** | Product       |
| 그래프 시각화     | **React Flow**    | Knowledge     |
| 코드 하이라이트   | **Shiki**         | 전체          |
| 페이지 전환       | **Framer Motion** | 전체          |

### 3.4 템플릿 커스터마이징

템플릿은 선택 후 `vibe.config.ts`에서 세부 조정:

```typescript
export default defineConfig({
  template: "developer",

  // 템플릿별 옵션
  templateOptions: {
    // Developer 템플릿 옵션
    showTerminal: true,
    terminalTheme: "dark",
    showGithubStars: true,
    packageManagers: ["npm", "pnpm", "yarn"],

    // 코드 블록
    codeTheme: "github-dark",
    showLineNumbers: true,
  },
});
```

---

## 4. User Experience

### 4.1 Quick Start

```bash
# 1. 프로젝트 생성
npx create-vibe-docs my-docs

# 2. 개발 서버 실행
cd my-docs
npm run dev

# 3. 배포
npm run build
```

### 4.2 사용자가 관리하는 파일 구조

```
my-docs/
├── docs/                    ← 마크다운 문서
│   ├── index.md
│   ├── getting-started.md
│   └── guides/
│       ├── installation.md
│       └── configuration.md
├── public/                  ← 정적 에셋
│   ├── logo.svg
│   └── og-image.png
├── vibe.config.ts           ← 설정 파일
└── package.json
```

### 4.3 Configuration

```typescript
// vibe.config.ts
import { defineConfig } from "vibe-docs";

export default defineConfig({
  // 기본 설정
  title: "My Documentation",
  description: "Welcome to my docs",

  // GitHub 연동 (편집 기능)
  github: {
    repo: "username/my-docs",
    branch: "main",
    editEnabled: true, // 웹 편집 활성화
    admins: ["username"], // 편집 권한자
  },

  // 테마
  theme: {
    colorScheme: "auto", // 'light' | 'dark' | 'auto'
    primaryColor: "blue", // 프리셋 컬러
    accentColor: "violet",
  },

  // 네비게이션
  nav: [
    { title: "Docs", href: "/docs" },
    { title: "GitHub", href: "https://github.com/..." },
  ],

  // 기능 토글
  features: {
    search: true,
    tableOfContents: true,
    editButton: true,
    lastUpdated: true,
  },
});
```

### 4.4 Frontmatter

```markdown
---
title: Getting Started
description: Learn how to set up VibeDocs
order: 1
---

# Getting Started

Your content here...
```

### 4.5 커스터마이징 레벨

| 레벨           | 방법                             | 자유도 |
| -------------- | -------------------------------- | ------ |
| **L1. 설정**   | `vibe.config.ts` 수정            | 낮음   |
| **L2. 스타일** | CSS 변수 오버라이드              | 중간   |
| **L3. 슬롯**   | 특정 영역에 커스텀 컴포넌트 삽입 | 중간   |
| **L4. 테마**   | 커뮤니티 테마 적용               | 중간   |
| **L5. Eject**  | 전체 소스 추출 후 직접 수정      | 높음   |

#### L2. CSS 변수 오버라이드

```css
/* public/custom.css */
:root {
  --vibe-primary: #3b82f6;
  --vibe-background: #fafafa;
  --vibe-sidebar-width: 280px;
  --vibe-font-sans: "Pretendard", sans-serif;
}
```

#### L3. 슬롯 시스템

```typescript
// vibe.config.ts
export default defineConfig({
  slots: {
    // 헤더 우측에 커스텀 버튼 추가
    "header-right": "./components/CustomButton.tsx",

    // 문서 하단에 피드백 위젯
    "doc-footer": "./components/FeedbackWidget.tsx",

    // 사이드바 상단에 로고
    "sidebar-top": "./components/Logo.tsx",
  },
});
```

---

## 5. Technical Architecture

### 5.1 하이브리드 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    VibeDocs Architecture                 │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  [일반 방문자]                [Admin]                    │
│       │                          │                       │
│       ▼                          ▼                       │
│  정적 파일 (CDN)           GitHub OAuth                  │
│       │                          │                       │
│       ▼                          ▼                       │
│  docs-data.json             GitHub API                   │
│  (빌드 시 생성)             (실시간 CRUD)                │
│       │                          │                       │
│       └──────────┬───────────────┘                       │
│                  ▼                                       │
│           VibeDocs Runtime                               │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 5.2 패키지 구조

```
@vibe-docs/
├── create-vibe-docs     # CLI (npx create-vibe-docs)
├── vibe-docs            # 코어 프레임워크
├── @vibe-docs/theme-default   # 기본 테마
└── @vibe-docs/plugin-*        # 공식 플러그인
```

### 5.3 Tech Stack

| 영역      | 기술              | 이유                     |
| --------- | ----------------- | ------------------------ |
| Framework | React + Vite      | 빠른 빌드, 풍부한 생태계 |
| Styling   | Tailwind CSS      | 테마 시스템과 호환       |
| Animation | Framer Motion     | 선언적 애니메이션        |
| Markdown  | unified ecosystem | 확장성                   |
| Auth      | GitHub OAuth      | 타겟 사용자 친화적       |
| Hosting   | Vercel/Netlify    | 무료, 간편 배포          |

### 5.4 Data Flow

#### Read (모든 사용자)

```
빌드: docs/*.md → docs-data.json
런타임: CDN → docs-data.json → 렌더링
✅ API 호출 없음, 초고속
```

#### Write (Admin)

```
편집 → GitHub API → 커밋 생성 → Actions 트리거 → 재배포
```

---

## 6. Milestones

### Phase 0: Foundation

**목표**: 프로젝트 기반 구축

- 모노레포 구조 (pnpm workspace)
- `create-vibe-docs` CLI 스캐폴딩
- `vibe-docs` 코어 패키지 구조
- TypeScript + ESLint + Prettier 설정

**산출물**: `npx create-vibe-docs my-docs` 실행 가능

---

### Phase 1: Static Docs

**목표**: 정적 문서 사이트 생성

- Markdown → HTML 빌드 파이프라인
- 기본 레이아웃 (Header, Sidebar, Content)
- 파일 기반 라우팅
- Frontmatter 파싱
- 자동 사이드바 생성
- 기본 테마

**산출물**: 읽기 전용 문서 사이트

---

### Phase 2: Design System

**목표**: 차별화된 디자인

- CSS 변수 기반 테마 시스템
- 다크모드
- 페이지 전환 애니메이션
- 코드 블록 스타일링
- 반응형 디자인
- 타이포그래피

**산출물**: 아름다운 문서 사이트

---

### Phase 3: GitHub Integration

**목표**: 인증 및 CRUD

- GitHub OAuth 플로우
- Admin 권한 체크
- 문서 Create/Update/Delete
- 커밋 메시지 입력
- 배포 상태 표시

**산출물**: 브라우저에서 문서 편집 가능

---

### Phase 4: Editor

**목표**: 편집 경험

- 마크다운 에디터 (CodeMirror)
- 실시간 프리뷰
- 툴바 (Bold, Link, Image...)
- 이미지 업로드
- 자동 저장

**산출물**: 생산적인 편집 환경

---

### Phase 5: Features

**목표**: 고급 기능

- 검색 (Cmd+K)
- Table of Contents
- 슬롯 시스템
- 플러그인 API
- 다국어 (i18n)

**산출물**: 완성도 높은 프레임워크

---

### Phase 6: Polish & Launch

**목표**: 출시 준비

- 성능 최적화
- SEO
- 문서 사이트 (dogfooding)
- npm 배포
- 랜딩 페이지

**산출물**: 오픈소스 공개

---

## 7. Key Decisions

### 7.1 프레임워크 vs 템플릿

| 옵션              | 장점                  | 단점                           |
| ----------------- | --------------------- | ------------------------------ |
| 템플릿            | 완전한 자유도         | 유지보수 부담, 업데이트 어려움 |
| **프레임워크** ✅ | 간편함, 자동 업데이트 | 커스터마이징 제한              |

**결론**: 프레임워크 방식 채택. 대부분의 사용자는 설정만으로 충분하며, 고급 사용자는 슬롯/eject 옵션 활용.

### 7.2 커스터마이징 전략

1. **설정으로 해결 가능한 것** → `vibe.config.ts`
2. **스타일 변경** → CSS 변수
3. **부분적 UI 변경** → 슬롯 시스템
4. **완전한 자유** → Eject

### 7.3 왜 GitHub API 직접 사용?

- ✅ 서버 비용 0원
- ✅ Git 히스토리 자동 생성
- ✅ GitHub가 인프라 관리
- ⚠️ Rate Limit → 읽기는 정적이라 OK

---

## 8. Security

| 위협               | 대응                           |
| ------------------ | ------------------------------ |
| client_secret 노출 | Serverless Function에서만 사용 |
| XSS                | DOMPurify로 마크다운 sanitize  |
| 권한 상승          | GitHub API가 repo 권한 검증    |

---

## 9. Risks

| 리스크            | 영향 | 대응                        |
| ----------------- | ---- | --------------------------- |
| GitHub API 변경   | 높음 | API 버전 명시, 래퍼 추상화  |
| 경쟁 솔루션       | 중간 | 디자인/UX 차별화 집중       |
| 커스터마이징 요구 | 중간 | 슬롯 시스템으로 확장성 제공 |

---

## 10. Open Questions

- [ ] 테마 마켓플레이스 지원 여부?
- [ ] 블로그 기능 포함 vs 별도 패키지?
- [ ] 플러그인 API 설계?
- [ ] Vercel 외 호스팅 지원 범위?

---

## 11. Appendix

### 11.1 참고 자료

- [Docusaurus](https://docusaurus.io/) - 구조 참고
- [VitePress](https://vitepress.dev/) - Vite 기반 참고
- [Nextra](https://nextra.site/) - Next.js 기반 참고
- [Mintlify](https://mintlify.com/) - 디자인 참고

### 11.2 용어 정의

| 용어  | 정의                                       |
| ----- | ------------------------------------------ |
| Admin | config에 등록된 GitHub 사용자 (편집 권한)  |
| Slot  | 사용자 컴포넌트를 삽입할 수 있는 지정 영역 |
| Eject | 프레임워크 내부 코드를 추출하여 직접 관리  |

---

**문서 버전**: 4.0
**최종 수정**: 2026-01-12
**상태**: 기획 완료
